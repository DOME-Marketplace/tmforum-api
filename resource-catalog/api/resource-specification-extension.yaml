openapi: 3.0.3
info:
  title: API ResourceSpecification extension
  version: 4.0.0
tags:
  - name: resourceSpecificationExtension
paths:
  /resourceSpecification/{id}:
    put:
      operationId: createResourceSpecificationWithId
      tags: [resourceSpecificationExtension]
      parameters:
        - name: id
          required: true
          in: path
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json;charset=utf-8:
            schema: { $ref: '#/components/schemas/ResourceSpecification_Create' }
      responses:
        '201':
          description: Created
          content:
            application/json;charset=utf-8:
              schema: { $ref: '#/components/schemas/ResourceSpecification' }
        '400': { description: Bad Request }
        '401': { description: Unauthorized }
        '403': { description: Forbidden }
        '405': { description: Method Not Allowed }
        '409': { description: Conflict }
        '500': { description: Internal Server Error }
components:
  schemas:
    ResourceSpecification:
      type: object
      properties:
        id: { type: string }
    ResourceSpecification_Create:
      type: object
      properties:
        name: { type: string }
