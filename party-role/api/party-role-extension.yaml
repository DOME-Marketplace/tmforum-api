openapi: 3.0.3
info:
  title: API PartyRole extension
  version: 4.0.0
tags:
  - name: partyRoleExtension
paths:
  /partyRole/{id}:
    put:
      operationId: createPartyRoleWithId
      tags: [partyRoleExtension]
      parameters:
        - name: id
          required: true
          in: path
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json;charset=utf-8:
            schema: { $ref: '#/components/schemas/PartyRole_Create' }
      responses:
        '201':
          description: Created
          content:
            application/json;charset=utf-8:
              schema: { $ref: '#/components/schemas/PartyRole' }
        '400': { description: Bad Request }
        '401': { description: Unauthorized }
        '403': { description: Forbidden }
        '405': { description: Method Not Allowed }
        '409': { description: Conflict }
        '500': { description: Internal Server Error }
components:
  schemas:
    PartyRole:
      type: object
      properties:
        id: { type: string }
    PartyRole_Create:
      type: object
      properties:
        name: { type: string }
