openapi: 3.0.3
info:
  title: API Service extension
  version: 4.0.0
tags:
  - name: serviceExtension
paths:
  /service/{id}:
    put:
      operationId: createServiceWithId
      tags: [serviceExtension]
      parameters:
        - name: id
          required: true
          in: path
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json;charset=utf-8:
            schema: { $ref: '#/components/schemas/Service_Create' }
      responses:
        '201':
          description: Created
          content:
            application/json;charset=utf-8:
              schema: { $ref: '#/components/schemas/Service' }
        '400': { description: Bad Request }
        '401': { description: Unauthorized }
        '403': { description: Forbidden }
        '405': { description: Method Not Allowed }
        '409': { description: Conflict }
        '500': { description: Internal Server Error }
components:
  schemas:
    Service:
      type: object
      properties:
        id: { type: string }
    Service_Create:
      type: object
      properties:
        name: { type: string }
