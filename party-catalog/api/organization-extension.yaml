openapi: 3.0.3
info:
  title: API Organization extension
  version: 4.0.0
tags:
  - name: organizationExtension
paths:
  /organization/{id}:
    put:
      operationId: createOrganizationWithId
      tags: [organizationExtension]
      parameters:
        - name: id
          required: true
          in: path
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json;charset=utf-8:
            schema: { $ref: '#/components/schemas/Organization_Create' }
      responses:
        '201':
          description: Created
          content:
            application/json;charset=utf-8:
              schema: { $ref: '#/components/schemas/Organization' }
        '400': { description: Bad Request }
        '401': { description: Unauthorized }
        '403': { description: Forbidden }
        '405': { description: Method Not Allowed }
        '409': { description: Conflict }
        '500': { description: Internal Server Error }
components:
  schemas:
    Organization:
      type: object
      properties:
        id: { type: string }
    Organization_Create:
      type: object
      properties:
        name: { type: string }
